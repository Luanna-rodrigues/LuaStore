import styled from "styled-components";
import { Typography } from "@material-ui/core";

const StyledTypTitle = styled(Typography)`
  && {
    display: flex;
    justify-content: center;
    font-size: 1.2em;
    font-weight: bold;
    padding-left: 15px;
    padding-bottom: 10px;
  }
`;

export default StyledTypTitle;
