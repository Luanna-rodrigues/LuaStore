import styled from "styled-components";
import { Card } from "@material-ui/core";

const StyledFavCard = styled(Card)`
  && {
    width: 100%;
    height: 300px;
    margin: 10px;
    margin-top: 30px;
    cursor: pointer;
  }
`;

export default StyledFavCard;
