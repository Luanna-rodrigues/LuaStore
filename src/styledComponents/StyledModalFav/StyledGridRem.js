import styled from "styled-components";
import { Grid } from "@material-ui/core";

const StyledGridRem = styled(Grid)`
  && {
    display: flex;
    justify-content: center;
    padding-top: 10px;
  }
`;

export default StyledGridRem;
