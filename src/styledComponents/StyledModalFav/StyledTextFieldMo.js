import styled from "styled-components";
import { TextField } from "@material-ui/core";

const StyledTextFieldMo = styled(TextField)`
  && {
    width: 20px;
    height: 3px;
  }
`;

export default StyledTextFieldMo;
