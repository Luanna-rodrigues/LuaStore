import styled from "styled-components";
import { Grid } from "@material-ui/core";

const StyledTotal = styled(Grid)`
  && {
    display: flex;
    justify-content: center;
    padding-bottom: 10px;
  }
`;

export default StyledTotal;
