import styled from "styled-components";
import { Grid } from "@material-ui/core";

const StyledGridSearch = styled(Grid)`
  && {
    display: flex;
    justify-content: center;
    align-items: center;
    padding-left: 8%;
  }
`;

export default StyledGridSearch;
