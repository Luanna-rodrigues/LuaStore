import styled from "styled-components";
import { Grid } from "@material-ui/core";

const StyledGridTitle = styled(Grid)`
  && {
    display: flex;
    align-items: center;
    padding-left: 20px;
  }
`;

export default StyledGridTitle;
