import styled from "styled-components";
import { Typography } from "@material-ui/core";

const StyledTypeRight = styled(Typography)`
  && {
    font-size: 1em;
    padding-top: 5px;
    padding-left: 20px;
    cursor: pointer;
  }
`;

export default StyledTypeRight;
