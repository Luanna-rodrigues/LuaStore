import styled from "styled-components";
import { Typography } from "@material-ui/core";

const StyledDescrFooter = styled(Typography)`
  && {
    font-size: 1em;
    padding-top: 10px;
    padding-right: 25px;
  }
`;

export default StyledDescrFooter;
