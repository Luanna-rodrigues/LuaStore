import styled from "styled-components";
import { Typography } from "@material-ui/core";

const StyledTypeLeft = styled(Typography)`
  && {
    font-size: 1em;
    padding-top: 5px;
    padding-left: 40px;
    cursor: pointer;
  }
`;

export default StyledTypeLeft;
