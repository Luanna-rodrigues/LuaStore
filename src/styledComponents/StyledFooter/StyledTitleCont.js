import styled from "styled-components";
import { Typography } from "@material-ui/core";

const StyledTitleCont = styled(Typography)`
  && {
    display: flex;
    justify-content: center;
    font-size: 1.5em;
    font-weight: bold;
    padding-bottom: 20px;
    padding-top: 50px;
  }
`;

export default StyledTitleCont;
