import styled from "styled-components";
import { Grid } from "@material-ui/core";

const StyledContainerFot = styled(Grid)`
  && {
    padding-bottom: 30px;
  }
`;

export default StyledContainerFot;
