import styled from "styled-components";
import { Grid } from "@material-ui/core";

const StyledGridNew = styled(Grid)`
 && {
    display: flex;
    justify-content: center;
    margin-top: 30px;
    margin-bottom: 50px;
 }
`;

export default StyledGridNew;
