import styled from "styled-components";
import { Typography } from "@material-ui/core";

const StyledTitleNew = styled(Typography)`
  && {
    justify-content: center;
    display: flex;
    font-size: 1.2em;
    padding-top: 10px;
    padding-right: 40px;
  }
`;

export default StyledTitleNew;
