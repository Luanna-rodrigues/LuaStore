import styled from "styled-components";
import { Grid } from "@material-ui/core";

const StyledGridGif = styled(Grid)`
  && {
    display: flex;
    justify-content: center;
    padding-bottom: 30px;
  }
`;

export default StyledGridGif;
