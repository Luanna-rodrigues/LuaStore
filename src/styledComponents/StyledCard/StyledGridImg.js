import styled from "styled-components";
import { Grid } from "@material-ui/core";

const StyledGridImg = styled(Grid)`
  && {
    display: flex;
    justify-content: center;
    padding-top: 15px;
    padding-bottom: 15px;
  }
`;

export default StyledGridImg;
