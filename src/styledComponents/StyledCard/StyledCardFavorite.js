import styled from "styled-components";
import { Card } from "@material-ui/core";

const StyledCardFavorite = styled(Card)`
  && {
    max-width: 400px;
    height: 500px;
    margin: 10px;
    margin-top: 30px;
  }
`;

export default StyledCardFavorite;
