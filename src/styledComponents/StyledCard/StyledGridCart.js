import styled from "styled-components";
import { Grid } from "@material-ui/core";

const StyledGridCart = styled(Grid)`
  && {
    display: flex;
    justify-content: end;
    padding-right: 10px;
  }
`;

export default StyledGridCart;
