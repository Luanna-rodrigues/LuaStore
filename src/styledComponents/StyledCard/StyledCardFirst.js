import styled from "styled-components";
import { Typography } from "@material-ui/core";

const StyledCardFirst = styled(Typography)`
  && {
    display: flex;
    justify-content: center;
    font-size: 1em;
    font-weight: bolder;
    padding-bottom: 20px;
    padding-top: 20px;
  }
`;

export default StyledCardFirst;
