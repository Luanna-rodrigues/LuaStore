import styled from "styled-components";
import { Typography } from "@material-ui/core";

const StyledCardPrice = styled(Typography)`
  && {
    display: flex;
    justify-content: center;
    font-size: 1em;
    font-weight: 500;
  }
`;

export default StyledCardPrice;
