import styled from "styled-components";
import { Grid } from "@material-ui/core";

const StyledButGrid = styled(Grid)`
  && {
    display: flex;
    justify-content: end;
    padding-right: 20px;
  }
`;

export default StyledButGrid;
