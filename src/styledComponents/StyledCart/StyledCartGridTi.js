import styled from "styled-components";
import { Grid } from "@material-ui/core";

const StyledCartGridTi = styled(Grid)`
  && {
    display: flex;
    padding-left: 20px;
    padding-bottom: 20px;
  }
`;

export default StyledCartGridTi;
