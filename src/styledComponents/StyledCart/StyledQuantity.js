import styled from "styled-components";
import { Grid } from "@material-ui/core";

const StyledQuantity = styled(Grid)`
  && {
    align-items: center;
    padding-left: 20px;
  }
`;

export default StyledQuantity;
