import styled from "styled-components";
import { Grid } from "@material-ui/core";

const StyledGridSub = styled(Grid)`
  && {
    display: flex;
    align-items: center;
    justify-content: space-around;
    padding-top: 10px;
  }
`;

export default StyledGridSub;
