import styled from "styled-components";
import { Typography } from "@material-ui/core";

const StyledTotalTyp = styled(Typography)`
  && {
    font-size: 1.3em;
    font-weight: 400;
  }
`;

export default StyledTotalTyp;
