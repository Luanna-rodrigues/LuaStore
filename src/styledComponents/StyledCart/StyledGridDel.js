import styled from "styled-components";
import { Grid } from "@material-ui/core";

const StyledGridDel = styled(Grid)`
  && {
    display: flex;
    justify-content: end;
  }
`;

export default StyledGridDel;
