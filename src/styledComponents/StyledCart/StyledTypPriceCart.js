import styled from "styled-components";
import { Typography } from "@material-ui/core";

const StyledTypPriceCart = styled(Typography)`
  && {
    padding-top: 50px;
    padding-left: 20px;
    font-size: 1em;
    font-weight: 100;
  }
`;

export default StyledTypPriceCart;
