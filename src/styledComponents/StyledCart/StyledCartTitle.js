import styled from "styled-components";
import { Typography } from "@material-ui/core";

const StyledCartTitle = styled(Typography)`
  && {
    padding-top: 50px;
    padding-left: 20px;
    font-size: 1.2em;
    font-weight: bold;
  }
`;

export default StyledCartTitle;
