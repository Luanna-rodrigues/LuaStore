import styled from "styled-components";
import { Grid } from "@material-ui/core";

const StyledFinish = styled(Grid)`
  && {
    display: flex;
    justify-content: center;
    padding-top: 50px;
  }
`;

export default StyledFinish;
