import styled from "styled-components";
import { Typography } from "@material-ui/core";

const StyledResume = styled(Typography)`
  && {
    display: flex;
    justify-content: center;
    padding-top: 20px;
    font-size: 1.5em;
    font-weight: bold;
  }
`;

export default StyledResume;
