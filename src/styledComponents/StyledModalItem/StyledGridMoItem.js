import styled from "styled-components";
import { Grid } from "@material-ui/core";

const StyledGridMoItem = styled(Grid)`
  && {
    display: flex;
    justify-content: center;
    margin: 20px;
  }
`;

export default StyledGridMoItem;
